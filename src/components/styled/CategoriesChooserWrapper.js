import styled from "styled-components";

const CategoriesChooserWrapper = styled.div`
  display: flex;
  flex-flow: column nowrap;
  justify-content: center;
  align-items: center;

  width: 600px;
  padding: 32px;

  background-color: white;
  border-radius: 6px;
  box-shadow: 0 0 1rem 0 rgba(0, 0, 0, 0.05);
`;

export default CategoriesChooserWrapper;
