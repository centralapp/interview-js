import FocusLock from "react-focus-lock";
import styled from "styled-components";

const FocusLocker = styled(FocusLock)`
  display: flex;
  flex-flow: column nowrap;

  width: 100%;
`;

export default FocusLocker;
